# upstream/

Draft contributions to other projects, discovered while building zonix.
Nothing here has been filed; each draft carries its own status header and is
presented for review first.

- `fastify-cliff/` — issue draft + repro-in-progress for the route-table-size
  throughput cliff (M2's counterpart obligation: report what we measure).
- `express-docs/` — docs PR draft: `req.is()` wildcard examples contradict the
  method's actual (and correctly prose-documented) return value.
